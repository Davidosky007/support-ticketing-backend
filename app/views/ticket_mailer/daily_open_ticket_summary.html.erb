<h1>Hello <%= @agent.name %>,</h1>

<p>You have <%= @tickets.count %> open tickets that require your attention:</p>

<table border="1" cellpadding="5" cellspacing="0">
  <thead>
    <tr>
      <th>ID</th>
      <th>Subject</th>
      <th>Customer</th>
      <th>Created At</th>
    </tr>
  </thead>
  <tbody>
    <% @tickets.each do |ticket| %>
      <tr>
        <td><%= ticket.id %></td>
        <td><%= ticket.subject %></td>
        <td><%= ticket.user.email %></td>
        <td><%= ticket.created_at.strftime("%Y-%m-%d %H:%M") %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>Please review and respond to these tickets as soon as possible.</p>

<p>Thank you,<br>
Head Of Support Team</p>